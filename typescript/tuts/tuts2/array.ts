const nums : number[] = [1, 2, 3, 4, 5];

// or another way to declare array in ts
const nums2 : Array<number> = [1, 2, 3, 4, 5];

console.log(nums);
console.log(nums2);

// array with mixed types
const mixed = ['John', 10, true];
console.log(mixed);
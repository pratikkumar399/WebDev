// tuples in typescript

// tuple is a collection of values of different types
// tuples are fixed in size
// tuples are ordered and like arrays, we can access the items using index
let tuple1: [string, number] = ["hello", 10];

// accessing the items of tuple using index
console.log(tuple1[0]);